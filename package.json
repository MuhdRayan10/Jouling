{
  "name": "jouling-mvp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Mobile-first territory game for verified energy-saving missions.",
  "scripts": {
    "build": "node scripts/copy-vendor.mjs",
    "start": "npm run build && node server/index.mjs",
    "dev": "npm run build && node --watch server/index.mjs",
    "test": "node --test tests/*.test.mjs",
    "qr": "node scripts/generate-qr.mjs"
  },
  "dependencies": {
    "jsqr": "^1.4.0",
    "maplibre-gl": "^6.6.0",
    "qrcode": "^1.5.4"
  },
  "engines": {
    "node": ">=20"
  }
}
