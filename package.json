{
  "name": "jouling-mvp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Mobile-first territory game for verified energy-saving missions.",
  "scripts": {
    "start": "node server/index.mjs",
    "dev": "node --watch server/index.mjs",
    "test": "node --test tests/*.test.mjs",
    "qr": "node scripts/generate-qr.mjs"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  },
  "engines": {
    "node": ">=20"
  }
}
